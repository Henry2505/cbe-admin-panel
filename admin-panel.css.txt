/* Background video */
#bg-video {
  position: fixed; top:0; left:0; width:100%; height:100%;
  object-fit: cover; z-index: -1;
}

/* Sidebar */
#sidebar {
  position: fixed; top:0; left:0; bottom:0;
  width: 240px; background: rgba(0,0,0,0.4);
  backdrop-filter: blur(8px); transition: width .3s;
  overflow: hidden;
}
#sidebar.collapsed { width: 60px; }
#sidebar .toggle-btn {
  padding: 1rem; cursor: pointer; color: #fff;
}
#sidebar nav ul { list-style:none; padding:0; margin: 2rem 0; }
#sidebar nav li {
  display: flex; align-items: center;
  padding: .75rem 1rem; color: #fff; cursor: pointer;
  transition: background .2s;
}
#sidebar nav li:hover { background: rgba(255,215,0,0.2); }
#sidebar nav li i { font-size: 1.2rem; margin-right: .75rem; }
#sidebar.collapsed nav li span { display: none; }

/* Main content */
main {
  margin-left: 240px; padding: 2rem; color: #fff;
  transition: margin-left .3s;
}
#sidebar.collapsed ~ main { margin-left: 60px; }
main section { display: none; }
main section.active { display: block; }

/* Tables & forms */
table {
  width: 100%; border-collapse: collapse; margin-top: 1rem;
}
thead th {
  background: rgba(255,215,0,0.4); padding: .75rem;
}
tbody td {
  background: rgba(0,0,0,0.5); padding: .65rem;
  border-top: 1px solid rgba(255,255,255,0.1);
}
button {
  background: rgba(255,215,0,0.6); border:none;
  padding: .5rem 1rem; cursor: pointer; margin-right:.5rem;
  transition: background .2s;
}
button:hover { background: rgba(255,215,0,0.8); }
input, select, textarea {
  width: 100%; padding: .5rem; margin: .5rem 0;
  background: rgba(0,0,0,0.4); border:1px solid rgba(255,215,0,0.6);
  color: #fff; outline: none;
}
.form-row { margin-bottom: 1rem; }

/* Reply panel */
#messageReply.hidden { display: none; }
#messageReply {
  background: rgba(0,0,0,0.6); padding: 1rem; margin-top:1rem;
}
.btn-group button { margin-right: .5rem; }

/* Trade History cards */
.history-card {
  background: rgba(0,0,0,0.5); padding:1rem; margin:1rem 0;
  border:1px solid rgba(255,215,0,0.4);
}
.history-card img { max-width:100%; display:block; margin-bottom:.5rem; }

/* Insights cards */
.insight-card {
  background: rgba(0,0,0,0.5); padding:1rem; margin:1rem 0;
  border:1px solid rgba(255,215,0,0.4);
}
